Black dashboard nav rail. Minimal, elegant spacing, red left-border indicator on the active item only (not decorative on every card).

```jsx
<Sidebar items={['Overview','Clientes','Relatórios','Financeiro']} active="Overview" />
```
