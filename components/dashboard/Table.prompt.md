Ultra-clean data table — typography-first, no heavy borders, subtle zebra striping.

```jsx
<Table columns={['Cliente','Canal','Investimento','ROAS']} rows={[['Tricard','Meta Ads','R$ 12.000','4.8x']]} />
```

Rows highlight on hover (`--surface-card-hover`); header labels are uppercase tracked small caps.
