import React from 'react';
/** @startingPoint section="Components" subtitle="Minimal cinematic line chart — intentional addition standing in for ApexCharts" viewport="700x120" */
export interface SparklineProps {
  data: number[];
  width?: number;
  height?: number;
  color?: string;
}
