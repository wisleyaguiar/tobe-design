Cinematic metric tile for dashboard overviews — huge serif number, small uppercase label, subtle hover.

```jsx
<KpiCard label="Faturamento" value="R$ 128k" delta="+12.4%" positive/>
```

Dark card background, thin border, no drop shadow by default. Value uses the display serif (Norman) at 40px; label is DuNord uppercase tracked text.
